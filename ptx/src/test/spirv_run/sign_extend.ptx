.version 6.5
.target sm_30
.address_size 64

.visible .entry sign_extend(
	.param .u32 input,
	.param .u32 output
)
{
	.reg .u32 	    in_addr;
    .reg .u32 	    out_addr;
    .reg .s32 	    temp;

	ld.param.u32 	in_addr, [input];
    ld.param.u32 	out_addr, [output];

    ld.s16          temp, [in_addr];
    st.s32          [out_addr], temp;
	ret;
}