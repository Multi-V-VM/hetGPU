.version 6.5
.target sm_30
.address_size 64

.visible .entry popc(
	.param .b32 input,
	.param .b32 output
)
{
    .reg .b32       temp;

	ld.param.b32 	temp, [input];
	popc.b32        temp, temp;
    st.param.b32    [output], temp;
	ret;
}
