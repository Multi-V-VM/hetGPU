.version 6.5
.target sm_30
.address_size 64

.visible .entry rcp(
	.param .f32 input,
	.param .f32 output
)
{
    .reg .f32 	    temp;

	ld.param.f32 	temp, [input];
	rcp.approx.f32  temp, temp;
    st.param.f32    [output], temp;
	ret;
}
