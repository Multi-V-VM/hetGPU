[package]
name = "zluda_cublas"
version = "0.0.0"
edition = "2021"

[lib]
name = "cublas"
crate-type = ["cdylib"]

[dependencies]
libc = "0.2"
num-complex = { version = "0.4", default-features = false }
num-traits = "0.2"

